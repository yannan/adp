apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.moduleName }}-svc
  namespace: {{ .Values.namespace }}
  labels:
    module: {{ .Values.moduleName }}
spec:
  selector:
    module: {{ .Values.moduleName }}
  type: {{ .Values.service.uniquery.type }}
  ports:
    - name: {{ .Values.moduleName }}
      port: {{ .Values.service.uniquery.port }}
      protocol: TCP
      targetPort: uniquery
      {{- if eq .Values.service.uniquery.type "NodePort" }}
      nodePort: {{ .Values.service.uniquery.nodePort }}
      {{- end }}