apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.moduleName }}-svc
  namespace: {{ .Values.namespace }}
  labels:
    module: {{ .Values.moduleName }}
spec:
  selector:
    module: {{ .Values.moduleName }}
  type: {{ .Values.service.vegagatewaypro.type }}
  ports:
    - name: {{ .Values.moduleName }}
      port: {{ .Values.service.vegagatewaypro.port }}
      protocol: TCP
      targetPort: vegagatewaypro
      {{- if eq .Values.service.vegagatewaypro.type "NodePort" }}
      nodePort: {{ .Values.service.vegagatewaypro.nodePort }}
      {{- end }}