<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eisoo.dc.common.metadata.mapper.FieldOldMapper">

    <select id="getFieldListByTableId" resultType="com.eisoo.dc.common.metadata.entity.FieldOldEntity">
        SELECT f_field_name FROM t_table_field
        WHERE f_table_id = #{tableId} AND f_delete_flag != 1
        <if test="keyword != null and keyword != ''">
            AND (f_field_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <select id="selectPage" resultType="com.eisoo.dc.common.metadata.entity.FieldOldEntity">
        SELECT * FROM t_table_field
        WHERE f_table_id = #{tableId} AND f_delete_flag != 1
        <if test="includeIds != null">
            AND f_field_name IN
            <foreach item="includeId" collection="includeIds" open="(" separator="," close=")">
                #{includeId}
            </foreach>
        </if>
        <if test="keyword != null and keyword != ''">
            AND (f_field_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY ${sortOrder} ${direction}
        <if test="limit > 0">
            LIMIT #{limit} OFFSET #{offset}
        </if>
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM t_table_field
        WHERE f_table_id = #{tableId} AND f_delete_flag != 1
        <if test="keyword != null and keyword != ''">
            AND f_field_name LIKE CONCAT('%', #{keyword}, '%')
        </if>
    </select>
    <delete id="deleteByDsId">
        DELETE
        FROM t_table_field
        WHERE f_table_id IN
              (SELECT f_id
               FROM t_table
               WHERE f_data_source_id = #{id})
    </delete>
</mapper>