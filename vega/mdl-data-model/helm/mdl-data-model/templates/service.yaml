apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.moduleName }}-svc
  namespace: {{ .Values.namespace }}
  labels:
    module: {{ .Values.moduleName }}
spec:
  selector:
    module: {{ .Values.moduleName }}
  type: {{ .Values.service.dataModel.type }}
  ports:
    - name: {{ .Values.moduleName }}
      port: {{ .Values.service.dataModel.port }}
      protocol: TCP
      targetPort: data-model
      {{- if eq .Values.service.dataModel.type "NodePort" }}
      nodePort: {{ .Values.service.dataModel.nodePort }}
      {{- end }}